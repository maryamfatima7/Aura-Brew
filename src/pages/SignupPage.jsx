import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { useApp } from '../context/AppContext'
export default function SignupPage() { const { signIn, showToast } = useApp(); const navigate = useNavigate(); const [form, setForm] = useState({ name: '', email: '', password: '', confirm: '' }); const [error, setError] = useState(''); const submit = (event) => { event.preventDefault(); if (!form.name.trim()) return setError('Please enter your name.'); if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(form.email)) return setError('Please enter a valid email.'); if (form.password.length < 6) return setError('Password must be at least 6 characters.'); if (form.password !== form.confirm) return setError('Passwords do not match.'); signIn(form.email, form.name); showToast('Welcome to AURA BREW.'); navigate('/') }; const update = (event) => setForm({ ...form, [event.target.name]: event.target.value }); return <section className="auth-page page-section"><div className="auth-wrap"><p className="eyebrow">AURA BREW / MEMBERS</p><h1 className="page-title">MAKE ROOM<br /><em>FOR RITUAL.</em></h1><form onSubmit={submit}><label>Full name<input name="name" value={form.name} onChange={update} required /></label><label>Email<input name="email" type="email" value={form.email} onChange={update} required /></label><label>Password<input name="password" type="password" value={form.password} onChange={update} required /></label><label>Confirm password<input name="confirm" type="password" value={form.confirm} onChange={update} required />{error && <small className="form-error">{error}</small>}</label><button className="button button-light" type="submit">Create account</button></form><p className="muted">Already a member? <Link to="/login">Sign in</Link></p></div></section> }
