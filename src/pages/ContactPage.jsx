import { useState } from 'react'
import { Mail, MapPin, Phone } from 'lucide-react'
import { useApp } from '../context/AppContext'
export default function ContactPage() { const { showToast } = useApp(); const [form, setForm] = useState({ name: '', email: '', message: '' }); const submit = (event) => { event.preventDefault(); showToast("Thanks for reaching out. We'll get back to you soon."); setForm({ name: '', email: '', message: '' }) }; return <section className="contact-page page-section"><p className="eyebrow">AURA BREW / SAY HELLO</p><h1 className="page-title">LET'S TALK<br /><em>COFFEE.</em></h1><div className="contact-grid"><div className="contact-info"><p>For collaborations, coffee questions, or a good story, find us here.</p><a href="mailto:hello@aurabrew.com"><Mail size={16} /> hello@aurabrew.com</a><a href="tel:+923000000000"><Phone size={16} /> +92 300 0000000</a><p><MapPin size={16} /> Karachi, Pakistan</p><p className="hours">Mon–Fri<br />7:00 AM — 9:00 PM<br /><br />Sat–Sun<br />8:00 AM — 10:00 PM</p></div><form className="contact-form" onSubmit={submit}><label>Name<input value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} required /></label><label>Email<input type="email" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} required /></label><label>Message<textarea value={form.message} onChange={(event) => setForm({ ...form, message: event.target.value })} required rows="5" /></label><button className="button button-light" type="submit">Send message</button></form></div></section> }
